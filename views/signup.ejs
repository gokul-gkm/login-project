<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign up</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="/static/login.css">
</head>
<body>
  <div class="full-screen-container">
    <div class="login-container">
      <h1 class="login-title">Signup</h1>
      <form class="form" action="/route/signup" method="post">
        <div class="input-group success">
          <label for="name">Name</label>
          <input type="text" name="username" id="name" required> 

          <% if (typeof messages !== 'undefined' && messages.error) { %>
            <span class="msg" style="color: red;"><%= messages.error %><span>
          <% } %>

        </div>
        <div class="input-group success">
          <label for="name">Email</label>
          <input type="email" name="email" id="email" required>

          <% if (typeof messages !== 'undefined' && messages.emailError) { %>
            <span class="msg" style="color: red;"><%= messages.emailError %><span>
          <% } %>

        </div>

        <div class="input-group error">
          <label for="password">Password</label>
          <input type="password" name="password" id="password" required>

          <% if (typeof messages !== 'undefined' && messages.passworderror) { %>
            <span class="msg" style="color: red;"><%= messages.passworderror %></span>
          <% } %>         
        </div>
        <div class="input-group error">
          <label for="password">Confirm Password</label>
          <input type="password" name="confirmpassword" id="confirmpassword" required>

          <% if (typeof messages !== 'undefined' && messages.confirmpasswordError) { %>
            <span class="msg" style="color: red;"><%= messages.confirmpasswordError %></span>
          <% } %>         
        </div>

        <button type="submit" class="login-button">Signup</button>
      </form>
      <a class="signup-link btn btn-dark rounded-pill border-blue bg-black" href="/">already have an account</a>
    </div>
  </div>
</body>
</html>